# @package _global_
defaults:
  - override /model: miglec-gcn-graphconv
  - override /trainer: semi-supervised-ensemble

trainer:
  init:
    optimizer:
      lr: 0.01
      weight_decay: 0.005
    scheduler:
      step_size: 1
      gamma: 0.975

# TODO migcer: remove
# logger:
#     disable: true
#   entity: miglec-n-a
#   project_name: semi-supervised-ensembles
